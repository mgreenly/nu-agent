# Development environment configuration for direnv
# This sets the database to a local project directory for development
export NUAGENT_DATABASE=./db/memory.db

# Test performance configuration
# Use in-memory database for fastest test execution (30-40% faster than file-based)
export TEST_DB_PATH=":memory:"

# Default to 4 parallel processes for test execution
export CORES=4
